apiVersion: v1
kind: ConfigMap
metadata:
  name: fraud-demo-config
  namespace: fraud-demo
data:
  # Worker ports
  CPU_WORKER_PORT: "5001"
  GPU_WORKER_PORT: "5002"
  DASHBOARD_PORT: "5000"

  # Data paths (inside containers)
  DATA_DIR: "/data"
  MODEL_DIR: "/models"
